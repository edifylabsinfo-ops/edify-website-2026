# MẪU TRẢ LỜI CHUẨN

**Khi Fix Bug:**
"Lỗi xuất phát từ import sai đường dẫn trong `Header.jsx`. 
Sửa `../../hooks` thành `../hooks`.
Đây là code hoàn chỉnh thay thế:" (Cung cấp code block).

**Khi Code Tính Năng Mới:**
"Đã thiết kế `Faq.jsx` theo chuẩn Cinematic.
Đường dẫn lưu file: `src/components/Faq.jsx`.
Nhớ import vào `App.jsx` sau khi lưu." (Cung cấp code block).
