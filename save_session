from weasyprint import HTML
import os

# Define the HTML content for the Cinematic Report
html_content = """
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <style>
        @page {
            size: A4;
            margin: 0;
            background-color: #163020;
        }
        body {
            margin: 0;
            padding: 40pt;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            color: #EEF0E5;
            background-color: #163020;
            line-height: 1.5;
        }
        .header {
            border-bottom: 1px solid rgba(238, 240, 229, 0.2);
            padding-bottom: 20pt;
            margin-bottom: 30pt;
        }
        .project-name {
            font-size: 24pt;
            letter-spacing: 0.3em;
            text-transform: uppercase;
            font-weight: bold;
            margin: 0;
        }
        .status-badge {
            display: inline-block;
            background-color: #EEF0E5;
            color: #163020;
            padding: 4pt 12pt;
            font-size: 10pt;
            font-weight: bold;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            margin-top: 10pt;
        }
        h2 {
            font-size: 16pt;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            border-left: 4pt solid #EEF0E5;
            padding-left: 10pt;
            margin-top: 30pt;
            margin-bottom: 15pt;
        }
        h3 {
            font-size: 12pt;
            color: rgba(238, 240, 229, 0.7);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-top: 20pt;
        }
        .code-block {
            background-color: rgba(0, 0, 0, 0.3);
            padding: 15pt;
            font-family: 'Courier New', Courier, monospace;
            font-size: 9pt;
            border: 1px solid rgba(238, 240, 229, 0.1);
            margin-bottom: 20pt;
            white-space: pre-wrap;
        }
        .log-entry {
            margin-bottom: 15pt;
            padding-left: 15pt;
            border-left: 1px solid rgba(238, 240, 229, 0.2);
        }
        .footer {
            margin-top: 50pt;
            font-size: 9pt;
            opacity: 0.4;
            text-align: center;
            letter-spacing: 0.2em;
            text-transform: uppercase;
        }
        .highlight {
            color: #EEF0E5;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="header">
        <p class="project-name">Edify Labs OS v2.0</p>
        <div class="status-badge">Session Snapshot: MAXPING</div>
        <p style="font-size: 10pt; opacity: 0.6; margin-top: 10pt;">Timestamp: 2026-04-20 10:30 AM</p>
    </div>

    <h2>1. Tổng quan Chiến dịch "Dọn dẹp Biển Đỏ"</h2>
    <div class="log-entry">
        <p><span class="highlight">Mục tiêu:</span> Khắc phục triệt để lỗi Build (Vercel) và đồng bộ hóa giao diện High-Ticket.</p>
        <p><span class="highlight">Kết quả:</span> Chuyển trạng thái từ 100% Build Fail (17/04) sang 100% Build Success (20/04).</p>
        <p><span class="highlight">Hạ tầng:</span> Nạp thành công MASTER GATEWAY (CLAUDE.md) làm bộ não điều phối dự án.</p>
    </div>

    <h2>2. Mã nguồn "Bọc thép" (Armored Code)</h2>
    
    <h3>A. Logic Ghi danh (src/leadService.js)</h3>
    <div class="code-block">
import { supabase } from './lib/supabase';
export const submitLead = async (data) => {
  try {
    const { data: result, error } = await supabase.from('leads').insert([{
      ...data, source_url: window.location.href, created_at: new Date().toISOString()
    }]);
    if (error) throw error;
    return { success: true, result };
  } catch (error) {
    return { success: false, error: error.message };
  }
};
    </div>

    <h3>B. Mắt thần UTM (src/hooks/useUTM.js)</h3>
    <div class="code-block">
export const useUTM = () => {
  const [utms, setUtms] = useState({ utm_source: '', utm_medium: '', utm_campaign: '' });
  useEffect(() => {
    const searchParams = new URLSearchParams(window.location.search);
    setUtms({
      utm_source: searchParams.get('utm_source') || '',
      utm_medium: searchParams.get('utm_medium') || '',
      utm_campaign: searchParams.get('utm_campaign') || ''
    });
  }, []);
  return utms;
};
    </div>

    <h2>3. Thiết kế Cinematic (Design Tokens Applied)</h2>
    <div class="log-entry">
        <p>- <span class="highlight">Màu sắc:</span> Deep Forest (#163020) & Soft Cream (#EEF0E5).</p>
        <p>- <span class="highlight">Quy tắc:</span> Rounded-none (Tuyệt đối không bo góc), font Serif tracking rộng.</p>
        <p>- <span class="highlight">Giao diện:</span> LeadForm & SuccessScreen đã được làm mới hoàn toàn theo phong cách tối giản sang trọng.</p>
    </div>

    <div class="footer">
        Generated by Edify Agent OS v2.0 | Conversion Hacker Mode Active
    </div>
</body>
</html>
"""

# Save the HTML to a temporary file
html_path = "session_snapshot.html"
pdf_path = "EDIFY_SESSION_SNAPSHOT_20260420.pdf"

with open(html_path, "w", encoding="utf-8") as f:
    f.write(html_content)

# Convert HTML to PDF
HTML(filename=html_path).write_pdf(pdf_path)

# Clean up temporary file
os.remove(html_path)

print(f"File generated: {pdf_path}")
